let kana=null;function permute(){let stored=localStorage.getItem("string"),permutation="";for(;stored&&stored.length>0;){let char=stored.charAt(Math.floor(Math.random()*stored.length));stored=stored.replace(char,""),permutation+=char}return permutation}async function initPractice(){const kanaUrl="js/kana.json";let response=await fetch(kanaUrl);kana=await response.json();let charsToLearn=permute();charsToLearn?(initInput(),startPractice(charsToLearn)):document.getElementById("selection-missing").classList.remove("d-none")}window.onload=initPractice;